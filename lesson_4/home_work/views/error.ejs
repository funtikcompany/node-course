<!DOCTYPE html>
<html lang="uk">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Помилка</title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
</head>
<body>
    <main>
        <div class="error-container">
            <h1><%= message %></h1>
            <% if (error.status) { %>
                <h2>Статус: <%= error.status %></h2>
            <% } %>
            <% if (error.stack) { %>
                <pre><%= error.stack %></pre>
            <% } %>
            <a href="/library" class="btn btn-primary" style="margin-top: 2rem;">Повернутися до бібліотеки</a>
        </div>
    </main>
</body>
</html>
